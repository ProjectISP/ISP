#!/bin/sh

if [ -f ak135.term ]; then rm ak135.term; fi
../bin/remodl ak135 > ak135.term
../bin/setbrn >> ak135.term
if [ -f iasp91.term ]; then rm iasp91.term; fi
../bin/remodl iasp91 > iasp91.term
../bin/setbrn >> iasp91.term
if [ -f nocrust.term ]; then rm nocrust.term; fi
../bin/remodl nocrust > nocrust.term
../bin/setbrn >> nocrust.term
mkdir -p stuff
mv setb* remod* stuff
